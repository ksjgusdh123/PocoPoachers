// <auto-generated>
using System.Collections.Generic;
using UnityEngine;

public class SoundTable
{
    static SoundTable _instance;
    public static SoundTable Instance => _instance ??= Load();
    readonly Dictionary<string, SoundData> _map = new Dictionary<string, SoundData>();

    static SoundTable Load()
    {
        var asset = ResourceManager.Instance.Load<TextAsset>("JsonData/sound");
        if (asset == null) { Debug.LogError("[SoundTable] not found: Resources/JsonData/sound.json"); return new SoundTable(); }
        var table = new SoundTable();
        string wrapped = "{\"items\":" + asset.text + "}";
        var wrapper = JsonUtility.FromJson<Wrapper>(wrapped);
        if (wrapper == null || wrapper.items == null) return table;
        foreach (var d in wrapper.items) table._map[d.key] = d;
        return table;
    }

    public SoundData Get(string key) => _map.TryGetValue(key, out var v) ? v : null;
    public IReadOnlyCollection<SoundData> All => _map.Values;

    [System.Serializable] class Wrapper { public List<SoundData> items = new List<SoundData>(); }
}
