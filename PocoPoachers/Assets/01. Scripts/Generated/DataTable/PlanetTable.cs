// <auto-generated>
using System.Collections.Generic;
using UnityEngine;

public class PlanetTable
{
    static PlanetTable _instance;
    public static PlanetTable Instance => _instance ??= Load();
    readonly Dictionary<int, PlanetData> _map = new Dictionary<int, PlanetData>();

    static PlanetTable Load()
    {
        var asset = ResourceManager.Instance.Load<TextAsset>("JsonData/planet");
        if (asset == null) { Debug.LogError("[PlanetTable] not found: Resources/JsonData/planet.json"); return new PlanetTable(); }
        var table = new PlanetTable();
        string wrapped = "{\"items\":" + asset.text + "}";
        var wrapper = JsonUtility.FromJson<Wrapper>(wrapped);
        if (wrapper == null || wrapper.items == null) return table;
        foreach (var d in wrapper.items) table._map[d.id] = d;
        return table;
    }

    public PlanetData Get(int key) => _map.TryGetValue(key, out var v) ? v : null;
    public IReadOnlyCollection<PlanetData> All => _map.Values;

    [System.Serializable] class Wrapper { public List<PlanetData> items = new List<PlanetData>(); }
}
