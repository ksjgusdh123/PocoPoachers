// <auto-generated>
using System.Collections.Generic;
using UnityEngine;

public class PlayerSkillTable
{
    static PlayerSkillTable _instance;
    public static PlayerSkillTable Instance => _instance ??= Load();
    readonly Dictionary<int, PlayerSkillData> _map = new Dictionary<int, PlayerSkillData>();

    static PlayerSkillTable Load()
    {
        var asset = ResourceManager.Instance.Load<TextAsset>("JsonData/player_skill");
        if (asset == null) { Debug.LogError("[PlayerSkillTable] not found: Resources/JsonData/player_skill.json"); return new PlayerSkillTable(); }
        var table = new PlayerSkillTable();
        string wrapped = "{\"items\":" + asset.text + "}";
        var wrapper = JsonUtility.FromJson<Wrapper>(wrapped);
        if (wrapper == null || wrapper.items == null) return table;
        foreach (var d in wrapper.items) table._map[d.id] = d;
        return table;
    }

    public PlayerSkillData Get(int key) => _map.TryGetValue(key, out var v) ? v : null;
    public IReadOnlyCollection<PlayerSkillData> All => _map.Values;

    [System.Serializable] class Wrapper { public List<PlayerSkillData> items = new List<PlayerSkillData>(); }
}
