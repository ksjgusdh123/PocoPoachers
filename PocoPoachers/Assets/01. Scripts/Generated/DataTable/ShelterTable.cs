// <auto-generated>
using System.Collections.Generic;
using UnityEngine;

public class ShelterTable
{
    static ShelterTable _instance;
    public static ShelterTable Instance => _instance ??= Load();
    readonly Dictionary<int, ShelterData> _map = new Dictionary<int, ShelterData>();

    static ShelterTable Load()
    {
        var asset = ResourceManager.Instance.Load<TextAsset>("JsonData/shelter");
        if (asset == null) { Debug.LogError("[ShelterTable] not found: Resources/JsonData/shelter.json"); return new ShelterTable(); }
        var table = new ShelterTable();
        string wrapped = "{\"items\":" + asset.text + "}";
        var wrapper = JsonUtility.FromJson<Wrapper>(wrapped);
        if (wrapper == null || wrapper.items == null) return table;
        foreach (var d in wrapper.items) table._map[d.id] = d;
        return table;
    }

    public ShelterData Get(int key) => _map.TryGetValue(key, out var v) ? v : null;
    public IReadOnlyCollection<ShelterData> All => _map.Values;

    [System.Serializable] class Wrapper { public List<ShelterData> items = new List<ShelterData>(); }
}
