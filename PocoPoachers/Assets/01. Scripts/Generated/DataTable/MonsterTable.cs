// <auto-generated>
using System.Collections.Generic;
using UnityEngine;

public class MonsterTable
{
    static MonsterTable _instance;
    public static MonsterTable Instance => _instance ??= Load();

    readonly Dictionary<int, MonsterData> _map = new Dictionary<int, MonsterData>();

    static MonsterTable Load()
    {
        var asset = Resources.Load<TextAsset>("Data/monster");
        if (asset == null) { Debug.LogError("[MonsterTable] Resources/Data/monster.json not found"); return new MonsterTable(); }
        var table = new MonsterTable();
        string wrapped = "{\"items\":" + asset.text + "}";
        var wrapper = JsonUtility.FromJson<Wrapper>(wrapped);
        foreach (var d in wrapper.items) table._map[d.id] = d;
        return table;
    }

    public MonsterData Get(int key) => _map.TryGetValue(key, out var v) ? v : null;
    public IReadOnlyCollection<MonsterData> All => _map.Values;

    [System.Serializable]
    class Wrapper { public List<MonsterData> items; }
}
