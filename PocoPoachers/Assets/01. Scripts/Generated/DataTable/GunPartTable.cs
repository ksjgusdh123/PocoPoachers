// <auto-generated>
using System.Collections.Generic;
using UnityEngine;

public class GunPartTable
{
    static GunPartTable _instance;
    public static GunPartTable Instance => _instance ??= Load();
    readonly Dictionary<int, GunPartData> _map = new Dictionary<int, GunPartData>();

    static GunPartTable Load()
    {
        var asset = ResourceManager.Instance.Load<TextAsset>("JsonData/gun_part");
        if (asset == null) { Debug.LogError("[GunPartTable] not found: Resources/JsonData/gun_part.json"); return new GunPartTable(); }
        var table = new GunPartTable();
        string wrapped = "{\"items\":" + asset.text + "}";
        var wrapper = JsonUtility.FromJson<Wrapper>(wrapped);
        if (wrapper == null || wrapper.items == null) return table;
        foreach (var d in wrapper.items) table._map[d.id] = d;
        return table;
    }

    public GunPartData Get(int key) => _map.TryGetValue(key, out var v) ? v : null;
    public IReadOnlyCollection<GunPartData> All => _map.Values;

    [System.Serializable] class Wrapper { public List<GunPartData> items = new List<GunPartData>(); }
}
